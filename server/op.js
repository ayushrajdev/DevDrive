a = {
    entity: 'event',
    account_id: 'acc_SrWlNVMlzS4W3n',
    event: 'subscription.charged',
    contains: ['subscription', 'payment'],
    payload: {
        subscription: {
            entity: {
                id: 'sub_SvUCkTuB2BoE49',
                entity: 'subscription',
                plan_id: 'plan_Suii8V3yAIThMT',
                customer_id: null,
                customer_email: 'ayushraj.dev10@gmail.com',
                customer_contact: '+917906477575',
                status: 'active',
                current_start: 1780124634,
                current_end: 1782757800,
                ended_at: null,
                quantity: 1,
                notes: [],
                charge_at: 1782757800,
                start_at: 1780124634,
                end_at: 1780124634,
                auth_attempts: 0,
                total_count: 1,
                paid_count: 1,
                customer_notify: true,
                created_at: 1780124582,
                expire_by: 1780140600,
                short_url: null,
                has_scheduled_changes: false,
                change_scheduled_at: null,
                source: 'dashboard',
                payment_method: 'card',
                offer_id: null,
                halted_at: null,
                remaining_count: 0,
            },
        },
        payment: {
            entity: {
                id: 'pay_SvUDf6bUK1xQti',
                entity: 'payment',
                amount: 8000,
                currency: 'INR',
                status: 'captured',
                order_id: 'order_SvUCl6slbbGroT',
                invoice_id: 'inv_SvUCl03rZYOIi4',
                international: false,
                method: 'card',
                amount_refunded: 0,
                amount_transferred: 0,
                refund_status: null,
                captured: '1',
                description: 'Start Subscription',
                card_id: 'card_SvUDfMJxIlNB6t',
                card: {
                    id: 'card_SvUDfMJxIlNB6t',
                    entity: 'card',
                    name: '',
                    last4: '4366',
                    network: 'VISA',
                    type: 'credit',
                    issuer: 'UTIB',
                    international: false,
                    emi: true,
                    expiry_month: '01',
                    expiry_year: '2099',
                    sub_type: 'consumer',
                    token_iin: '471860910',
                    number: '**** **** **** 4366',
                    color: '#C15482',
                },
                bank: null,
                wallet: null,
                vpa: null,
                email: 'ayushraj.dev10@gmail.com',
                contact: '+917906477575',
                customer_id: null,
                token_id: 'token_SvUDfYAtEuOijL',
                notes: { dashboard: 'true' },
                fee: 232,
                tax: 0,
                error_code: null,
                error_description: null,
                acquirer_data: { auth_code: '597758' },
                created_at: 1780124634,
            },
        },
    },
    created_at: 1780124650,
};

a = {
    entity: 'event',
    account_id: 'acc_SrWlNVMlzS4W3n',
    event: 'subscription.activated',
    contains: ['subscription', 'payment'],
    payload: {
        subscription: {
            entity: {
                id: 'sub_SvUCkTuB2BoE49',
                entity: 'subscription',
                plan_id: 'plan_Suii8V3yAIThMT',
                customer_id: null,
                customer_email: 'ayushraj.dev10@gmail.com',
                customer_contact: '+917906477575',
                status: 'active',
                current_start: 1780124634,
                current_end: 1782757800,
                ended_at: null,
                quantity: 1,
                notes: [],
                charge_at: 1782757800,
                start_at: 1780124634,
                end_at: 1780124634,
                auth_attempts: 0,
                total_count: 1,
                paid_count: 1,
                customer_notify: true,
                created_at: 1780124582,
                expire_by: 1780140600,
                short_url: null,
                has_scheduled_changes: false,
                change_scheduled_at: null,
                source: 'dashboard',
                payment_method: 'card',
                offer_id: null,
                halted_at: null,
                remaining_count: 0,
            },
        },
        payment: {
            entity: {
                id: 'pay_SvUDf6bUK1xQti',
                entity: 'payment',
                amount: 8000,
                currency: 'INR',
                status: 'captured',
                order_id: 'order_SvUCl6slbbGroT',
                invoice_id: 'inv_SvUCl03rZYOIi4',
                international: false,
                method: 'card',
                amount_refunded: 0,
                amount_transferred: 0,
                refund_status: null,
                captured: '1',
                description: 'Start Subscription',
                card_id: 'card_SvUDfMJxIlNB6t',
                card: {
                    id: 'card_SvUDfMJxIlNB6t',
                    entity: 'card',
                    name: '',
                    last4: '4366',
                    network: 'VISA',
                    type: 'credit',
                    issuer: 'UTIB',
                    international: false,
                    emi: true,
                    expiry_month: '01',
                    expiry_year: '2099',
                    sub_type: 'consumer',
                    token_iin: '471860910',
                    number: '**** **** **** 4366',
                    color: '#C15482',
                },
                bank: null,
                wallet: null,
                vpa: null,
                email: 'ayushraj.dev10@gmail.com',
                contact: '+917906477575',
                customer_id: null,
                token_id: 'token_SvUDfYAtEuOijL',
                notes: { dashboard: 'true' },
                fee: 232,
                tax: 0,
                error_code: null,
                error_description: null,
                acquirer_data: { auth_code: '597758' },
                created_at: 1780124634,
            },
        },
    },
    created_at: 1780124650,
};

c = {
    entity: 'event',
    account_id: 'acc_SrWlNVMlzS4W3n',
    event: 'subscription.completed',
    contains: ['subscription', 'payment'],
    payload: {
        subscription: {
            entity: {
                id: 'sub_SvUCkTuB2BoE49',
                entity: 'subscription',
                plan_id: 'plan_Suii8V3yAIThMT',
                customer_id: null,
                customer_email: 'ayushraj.dev10@gmail.com',
                customer_contact: '+917906477575',
                status: 'completed',
                current_start: 1780124634,
                current_end: 1782757800,
                ended_at: 1780124634,
                quantity: 1,
                notes: [],
                charge_at: null,
                start_at: 1780124634,
                end_at: 1780124634,
                auth_attempts: 0,
                total_count: 1,
                paid_count: 1,
                customer_notify: true,
                created_at: 1780124582,
                expire_by: 1780140600,
                short_url: null,
                has_scheduled_changes: false,
                change_scheduled_at: null,
                source: 'dashboard',
                payment_method: 'card',
                offer_id: null,
                halted_at: null,
                remaining_count: 0,
            },
        },
        payment: {
            entity: {
                id: 'pay_SvUDf6bUK1xQti',
                entity: 'payment',
                amount: 8000,
                currency: 'INR',
                status: 'captured',
                order_id: 'order_SvUCl6slbbGroT',
                invoice_id: 'inv_SvUCl03rZYOIi4',
                international: false,
                method: 'card',
                amount_refunded: 0,
                amount_transferred: 0,
                refund_status: null,
                captured: '1',
                description: 'Start Subscription',
                card_id: 'card_SvUDfMJxIlNB6t',
                card: {
                    id: 'card_SvUDfMJxIlNB6t',
                    entity: 'card',
                    name: '',
                    last4: '4366',
                    network: 'VISA',
                    type: 'credit',
                    issuer: 'UTIB',
                    international: false,
                    emi: true,
                    expiry_month: '01',
                    expiry_year: '2099',
                    sub_type: 'consumer',
                    token_iin: '471860910',
                    number: '**** **** **** 4366',
                    color: '#C15482',
                },
                bank: null,
                wallet: null,
                vpa: null,
                email: 'ayushraj.dev10@gmail.com',
                contact: '+917906477575',
                customer_id: null,
                token_id: 'token_SvUDfYAtEuOijL',
                notes: { dashboard: 'true' },
                fee: 232,
                tax: 0,
                error_code: null,
                error_description: null,
                acquirer_data: { auth_code: '597758' },
                created_at: 1780124634,
            },
        },
    },
    created_at: 1780124650,
};

d = {
    entity: 'event',
    account_id: 'acc_SrWlNVMlzS4W3n',
    event: 'subscription.authenticated',
    contains: ['subscription'],
    payload: {
        subscription: {
            entity: {
                id: 'sub_SvUCkTuB2BoE49',
                entity: 'subscription',
                plan_id: 'plan_Suii8V3yAIThMT',
                customer_id: null,
                customer_email: 'ayushraj.dev10@gmail.com',
                customer_contact: '+917906477575',
                status: 'completed',
                current_start: 1780124634,
                current_end: 1782757800,
                ended_at: 1780124634,
                quantity: 1,
                notes: [],
                charge_at: null,
                start_at: 1780124634,
                end_at: 1780124634,
                auth_attempts: 0,
                total_count: 1,
                paid_count: 1,
                customer_notify: true,
                created_at: 1780124582,
                expire_by: 1780140600,
                short_url: null,
                has_scheduled_changes: false,
                change_scheduled_at: null,
                source: 'dashboard',
                payment_method: 'card',
                offer_id: null,
                halted_at: null,
                remaining_count: 0,
            },
        },
    },
    created_at: 1780124650,
};











































































import StripePaymentProvider from '../providers/payment/stripe.provider.js';
import MongoSubscriptionRepository from '../repositories/mongodb/subscription.repository.js';

export default class StripeWebhooks {
    constructor() {
        this.stripeService = new StripePaymentProvider();
        this.subscriptionRepository = new MongoSubscriptionRepository();
    }

    handleSubscriptions = async (req, res) => {
        // req.body must be raw Buffer — register this route with express.raw()
        const sig = req.headers['stripe-signature'];
        let event;

        try {
            event = this.stripeService.stripe.webhooks.constructEvent(
                req.body,
                sig,
                process.env.STRIPE_WEBHOOK_SECRET,
            );
        } catch (err) {
            console.error('Stripe webhook signature verification failed:', err.message);
            return res.status(400).send(`Webhook Error: ${err.message}`);
        }

        try {
            switch (event.type) {
                case 'checkout.session.completed': {
                    await this.#handleCheckoutSessionCompleted(event.data.object);
                    break;
                }

                case 'invoice.payment_succeeded': {
                    await this.#handleInvoicePaymentSucceeded(event.data.object);
                    break;
                }

                case 'invoice.payment_failed': {
                    await this.#handleInvoicePaymentFailed(event.data.object);
                    break;
                }

                case 'customer.subscription.updated': {
                    await this.#handleSubscriptionUpdated(event.data.object);
                    break;
                }

                case 'customer.subscription.deleted': {
                    await this.#handleSubscriptionDeleted(event.data.object);
                    break;
                }

                default:
                    console.log(`Unhandled Stripe event type: ${event.type}`);
            }

            return res.json({ received: true });
        } catch (err) {
            console.error(`Error handling Stripe event ${event.type}:`, err);
            // Still return 200 so Stripe doesn't retry — log and handle internally
            return res.status(500).json({ error: 'Internal handler error' });
        }
    };

    // ─── Private Handlers ────────────────────────────────────────────────────

    #handleCheckoutSessionCompleted = async (session) => {
        const {
            id,
            mode,
            customer,           // Stripe customer ID e.g. cus_xxx
            subscription,       // Present only when mode === 'subscription'
            payment_intent,     // Present only when mode === 'payment'
            metadata,
            payment_status,
            client_reference_id,
        } = session;

        // Guard: only proceed if payment is actually paid
        if (payment_status !== 'paid') {
            console.warn(`checkout.session.completed with payment_status: ${payment_status}, skipping.`);
            return;
        }

        const { userId, planId, paymentType, billingCycle } = metadata;

        switch (mode) {
            case 'payment': {
                // One-time / lifetime payment
                await this.subscriptionRepository.createSubcription({
                    userId,
                    planId,
                    paymentType,            // 'one_time'
                    billingCycle,           // 'lifetime'
                    provider: 'stripe',
                    providerSubscriptionId: payment_intent ?? id,  // no subscription object for one-time
                    providerCustomerId: customer,
                    status: 'active',
                    currentPeriodStart: new Date(),
                    currentPeriodEnd: null, // lifetime — never expires
                    cancelAtPeriodEnd: false,
                });
                break;
            }

            case 'subscription': {
                // Subscription payment — Stripe will also fire invoice.payment_succeeded
                // so we just record the subscription mapping here; full activation
                // happens on invoice.payment_succeeded to avoid double-processing.
                // However, we create the record here in 'pending' state and activate it there.
                await this.subscriptionRepository.createSubcription({
                    userId,
                    planId,
                    paymentType,            // 'subscription'
                    billingCycle,
                    provider: 'stripe',
                    providerSubscriptionId: subscription, // sub_xxx
                    providerCustomerId: customer,
                    status: 'pending',      // activated on invoice.payment_succeeded
                    currentPeriodStart: new Date(),
                    currentPeriodEnd: null, // filled in on invoice.payment_succeeded
                    cancelAtPeriodEnd: false,
                });
                break;
            }

            default:
                console.warn(`Unknown checkout session mode: ${mode}`);
        }
    };

    #handleInvoicePaymentSucceeded = async (invoice) => {
        const {
            subscription,           // sub_xxx
            customer,               // cus_xxx
            period_start,
            period_end,
            billing_reason,         // 'subscription_create' | 'subscription_cycle' | 'subscription_update'
        } = invoice;

        if (!subscription) {
            // One-time invoice, nothing to update
            return;
        }

        const currentPeriodStart = new Date(period_start * 1000);
        const currentPeriodEnd = new Date(period_end * 1000);

        if (billing_reason === 'subscription_create') {
            // First payment — activate the subscription created in checkout.session.completed
            await this.subscriptionRepository.updateByProviderSubscriptionId(
                subscription,
                {
                    status: 'active',
                    currentPeriodStart,
                    currentPeriodEnd,
                },
            );
        } else if (billing_reason === 'subscription_cycle') {
            // Recurring renewal — extend the period
            await this.subscriptionRepository.updateByProviderSubscriptionId(
                subscription,
                {
                    status: 'active',
                    currentPeriodStart,
                    currentPeriodEnd,
                },
            );
        }
    };

    #handleInvoicePaymentFailed = async (invoice) => {
        const { subscription, next_payment_attempt } = invoice;

        if (!subscription) return;

        // If Stripe has exhausted all retries, next_payment_attempt will be null
        const status = next_payment_attempt ? 'past_due' : 'failed';

        await this.subscriptionRepository.updateByProviderSubscriptionId(
            subscription,
            { status },
        );

        // TODO: trigger a notification to the user here
    };

    #handleSubscriptionUpdated = async (stripeSubscription) => {
        const {
            id,
            status,
            current_period_start,
            current_period_end,
            cancel_at_period_end,
            pause_collection,
            trial_end,
        } = stripeSubscription;

        const update = {
            currentPeriodStart: new Date(current_period_start * 1000),
            currentPeriodEnd: new Date(current_period_end * 1000),
            cancelAtPeriodEnd: cancel_at_period_end,
            trialEndsAt: trial_end ? new Date(trial_end * 1000) : null,
        };

        if (pause_collection) {
            update.status = 'paused';
        } else if (status === 'active') {
            update.status = 'active';
        } else if (status === 'past_due' || status === 'unpaid') {
            update.status = 'past_due';
        } else if (status === 'trialing') {
            update.status = 'active'; // treat trialing as active in your system
        }

        await this.subscriptionRepository.updateByProviderSubscriptionId(id, update);
    };

    #handleSubscriptionDeleted = async (stripeSubscription) => {
        const { id, ended_at } = stripeSubscription;

        await this.subscriptionRepository.updateByProviderSubscriptionId(id, {
            status: 'cancelled',
            cancelledAt: ended_at ? new Date(ended_at * 1000) : new Date(),
            cancelAtPeriodEnd: false,
        });

        // TODO: revoke user access here
    };
}